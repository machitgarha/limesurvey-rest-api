access_token:
  type: string
  description: Bearer access token value.

token_info:
  type: object
  properties:
    access_token:
      $ref: "#/access_token"
    expire_time:
      type: integer
      description: The absolute Unix time in which the token expires.
