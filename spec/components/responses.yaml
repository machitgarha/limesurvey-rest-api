empty_ok:
  description: OK

invalid_credentials:
  description: Invalid username or password
  content:
    application/json:
      schema:
        type: object
        properties:
          errors:
            type: array
            minItems: 1
            maxItems: 1
            items:
              $ref: "./schemas/error.yaml#/invalid_credentials"

method_not_allowed:
  description: Method not allowed

not_found:
  description: Resource not found

permission_denied:
  description: Permission denied

unauthorized:
  description: Unauthorized request
  content:
    application/json:
      schema:
        type: object
        properties:
          errors:
            type: array
            minItems: 1
            # TODO:
            # maxItems: 1
            items:
              $ref: "./schemas/error.yaml#/unauthorized_error"
